// Converted to legacy config file at `.eslintrc.js`. This placeholder prevents ESLint from treating
// this as a flat config file. Keeping it empty to avoid conflicts in different ESLint loaders.
module.exports = {};
