/**
 * Loader configuration settings
 */
export const LOADER_CONFIG = {
  // Minimum time (ms) the loader should be visible to prevent flickering
  MIN_LOADING_TIME: 800,

  // Delay (ms) before starting the outro animation after page is ready
  OUTRO_DELAY: 500,

  // Delay (ms) before triggering page animations after loader is hidden
  PAGE_ANIMATION_DELAY: 100,

  // Maximum time (ms) to wait for initial load before forcing completion
  MAX_INITIAL_LOAD_TIME: 5000,
};
